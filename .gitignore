# macOS 시스템 파일
.DS_Store

# 에디터 설정
.vscode/
.idea/

# 로그
*.log

# Claude Code 로컬 권한 설정(개인 환경용이라 공유하지 않음)
.claude/settings.local.json

# 실제 배포용 비밀값이 담길 수 있는 로컬 메모
*.local

# Playwright MCP 검증 산출물 (스크린샷·콘솔 로그·스냅샷)
.playwright-mcp/
*.png

# 기획 문서 — 로컬에만 보관 (public 리포 공개 제외, 발주자 결정)
매도사다리_기획서.md

# Next.js
node_modules/
.next/
out/
*.tsbuildinfo

# 로컬 환경변수 (비밀값 — 절대 커밋하지 않는다)
.env*.local
