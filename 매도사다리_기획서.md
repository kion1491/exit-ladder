# 매도 사다리 — 분할익절 역산 계산기 기획서

- 작성일: 2026-08-23
- 발주자: 카이온
- 구현 담당: Claude Code
- 이 문서는 발주자와 Claude(챗)가 grill-me 방식으로 확정한 결정사항의 최종본이다. 문서에 없는 것을 임의로 추가하지 말고, 모호한 부분은 구현 전에 질문할 것.

---

## 1. 한 줄 정의

매수가·손절가·원하는 손익비·분할 수를 입력하면, **그 손익비를 만족하는 분할 매도가격 사다리를 역산해서 보여주는** 모바일 웹 계산기. 사용자는 출력된 최종 매도가가 현실적인지(차트·펀더멘털)를 보고 진입/포기를 판단한다.

## 2. 설계 철학 (구현 판단이 갈릴 때 이 절을 기준으로)

1. **역산 구조가 핵심이다.** 기존 도구(목표가 입력 → 손익비 계산)를 뒤집었다. 손익비가 입력, 목표가(매도 사다리)가 출력이다. 역DCF처럼 "이 거래가 성립하려면 주가가 어디까지 가야 하는데, 그게 말이 되나?"를 사람이 판정하게 만든다.
2. **기계는 산수, 사람은 도달 가능성 판단.** 도구는 확률을 모른다. 확률 판단을 돕는 정보(필요 상승률, 상한가 대비 판정)만 정직하게 노출한다.
3. **앵커링 방어.** 도구가 찍어준 가격을 보면 "갈 수 있지"로 뇌가 기운다. 최종가까지 필요한 상승률 %를 눈에 띄게 표시하고, 상한가 입력 시 초과 여부를 도구가 먼저 선언한다.
4. **이 도구는 진입·청산 계산기다.** 매매 전체를 지휘하지 않는다. 매수는 전량 일괄(분할매수 없음 — 분할매수는 별도 스프레드시트 v2.1의 영역), 보유 중 관리는 "+1R 본전 스탑" 규칙 한 줄만 안내한다.
5. **심플함이 기능이다.** 필수 입력 4개(매수가·손절가·분할수·손익비). 나머지는 전부 선택.

## 3. 사용 흐름

1. 매수가·손절가 입력 (시장 KR/US 선택)
2. 분할 수 선택 (1 / 2 / 3분할)
3. 원하는 손익비 입력 (슬라이더 + 직접 입력)
4. 출력 확인: 매도가격들 + 비중 % (+예산 입력 시 주수·순손익)
5. 사람이 판정: 최종 매도가가 현실적인가 → 아니면 포기, 맞으면 수행. 선택 입력 "현실 상한가"를 채우면 이 판정을 도구가 배지로 보조
6. (선택) "이 계획 저장" → 구글시트에 스냅샷 한 줄 적재, "저장 목록"에서 조회

## 4. 입력 명세

| 필드 | 필수 | 형식 | 검증/비고 |
|---|---|---|---|
| 종목명 | 선택 | 텍스트 | 저장용. 비어 있으면 "(무명)"으로 저장 |
| 시장 | 필수 | KR / US 토글 | 기본 KR. 호가단위·가격 표기 결정 |
| 매수가 E | 필수 | 숫자 > 0 | |
| 손절가 S | 필수 | 숫자 > 0 | **S < E** 아니면 에러 메시지("손절가는 매수가보다 낮아야 합니다") 표시하고 결과 미출력 |
| 분할 수 n | 필수 | 1 / 2 / 3 토글 | 기본 3 |
| 손익비 k | 필수 | 슬라이더 1.0~5.0, step 0.1 + 숫자 직접입력(1.0~9.9) 동기화 | 기본 2.0. 변경 즉시 결과 실시간 갱신 |
| 예산 | 선택 | 숫자(원) | 채우면 주수·금액·순손익 표시 |
| 현실 상한가 C | 선택 | 숫자 | 채우면 이내/초과 배지 판정 (5.5절) |
| 메모 | 선택 | 텍스트 한 줄 | 저장용 |

## 5. 계산 명세

### 5.1 기호
- R = E − S (1주당 리스크)
- 손절폭 = R / E
- k = 입력 손익비, n = 분할 수

### 5.2 매도 사다리 공식 (확정 구조: 균등 비중 + 1차 +1R 고정 + 등간격)

| n | 매도가 (호가 내림 전) | 비중 |
|---|---|---|
| 1분할 | E + kR | 100% |
| 2분할 | E + R, E + (2k−1)R | 50% / 50% |
| 3분할 | E + R, E + kR, E + (2k−1)R | 33% / 33% / 34% |

- 검증 항등식: 비중 가중평균 매도가 = E + kR (내림 전 기준). 호가 내림 후에는 미세하게 k 이하가 되며 이는 보수적이므로 허용.
- k = 1.0이고 n ≥ 2이면 가격이 겹친다(전부 +1R). 에러 아님 — 그대로 표시.

### 5.3 호가단위 (매도가는 항상 **내림** — 보수적)

- KR: 가격 p 기준 — p<2,000→1원 / p<5,000→5원 / p<20,000→10원 / p<50,000→50원 / p<200,000→100원 / p<500,000→500원 / 이상→1,000원. `floor(p / tick) * tick` (부동소수 오차 보정: `floor(p/tick + 1e-9)`)
- US: 센트 내림. `floor(p*100 + 1e-6) / 100`
- 표기: KR 정수 천단위 콤마, US 소수 둘째 자리 고정

### 5.4 파생 지표 (판정 칩으로 표시)

| 지표 | 공식 | 표시 규칙 |
|---|---|---|
| 손절폭 | R/E | 2% 미만 → "노이즈 주의"(경고색), 10% 초과 → "타점 늦음"(위험색), 그 외 중립 |
| 본전 승률 | 1/(1+k) | 정수 % ("본전 승률 33%") |
| 필요 상승률 | (최종 매도가 − E)/E | "+18.0% 필요". 30% 초과 시 경고색 |

### 5.5 현실 상한가 판정 (C 입력 시에만)

- 각 매도가에 배지: 매도가 ≤ C → **이내**, 초과 → **초과**
- C ≤ E → "상한가가 매수가 이하 — 이 자리는 들어갈 이유가 없습니다."
- 최종 매도가 > C → 달성 가능 최대 손익비를 역산해 제안:
  - k_max = (C−E)/R (1분할) 또는 ((C−E)/R + 1) / 2 (2·3분할)
  - 소수 첫째 자리 **내림**
  - k_max ≥ 1.0 → "최종가가 상한을 넘습니다 — 이 자리에서 가능한 손익비는 {k_max}까지. 그걸로 부족하면 이 거래는 포기."
  - k_max < 1.0 → "이 상한으로는 손익비 1.0도 안 나옵니다 — 진입 포기 권장."
- 전부 이내 → "모든 매도가가 상한 이내 — 계획 성립. 마지막 판단은 당신 몫."

### 5.6 예산 계산 (예산 입력 시에만)

- 총수량 qty = floor(예산 / E)  ← 전량 일괄 매수이므로 E 기준
- 분할 주수: n=1 → [qty] / n=2 → [floor(qty/2), 나머지] / n=3 → [floor(qty/3), floor(qty/3), 나머지]
- 왕복 거래비용 FEE = 0.3% 고정. 매수·매도 금액에 절반씩(0.15%) 적용
- 손절 시 순손익 = qty·S − qty·E − (qty·S + qty·E)·FEE/2
- 전량 익절 시 순손익 = Σ(주수ᵢ·매도가ᵢ) − qty·E − (Σ(주수ᵢ·매도가ᵢ) + qty·E)·FEE/2
- **매도가 자체에는 수수료를 섞지 않는다** (호가 깔끔 유지). 순손익 표시에만 반영

## 6. 출력·UI 요구사항

- **모바일 우선** (기준 폭 ~390px), 한국어, 단일 화면. 데스크톱은 중앙 정렬 max-width로 충분
- **색상 관습: 한국 증시 기준.** 이익/매도가/상승 = 빨강 계열, 손실/손절 = 파랑 계열. (서구식 초록/빨강 금지)
- **시그니처 요소 — 수직 가격 레일**: 손절가(하단, 파랑) → 매수가(기준선) → 매도가 1~n차(빨강 눈금) 를 실제 가격 비례 위치로 배치. 상한가 입력 시 주황 점선 가로선으로 표시하고, 이를 넘는 눈금이 시각적으로 드러나야 함. 슬라이더 조작 시 눈금이 부드럽게 이동(transition, prefers-reduced-motion 존중)
- 각 매도 눈금 라벨: 가격 · {i}차 · 비중% · (주수) · 매수가 대비 +%
- 판정 칩 스트립(5.4)은 레일 위쪽에 배치
- 고정 규칙 문구(항상 표시): **"1차 매도가(+1R) 체결 시 손절을 본전으로. 손절 이동은 위로만. 최종가가 비현실적이면 이 거래는 하지 않는다."**
- 하단 고지: "순손익은 왕복 거래비용 0.3% 반영 · 매도가는 호가단위 내림"
- 입력 즉시 재계산 (저장 버튼 외에 별도 '계산' 버튼 없음)
- 초기 로드 시 예시값 채움: E=10,000, S=9,400, k=2.0, 3분할

## 7. 저장 명세 (Google Sheets + Apps Script)

### 7.1 아키텍처
- 프론트: 정적 페이지 (GitHub Pages)
- 백엔드: 구글시트에 바인딩된 Apps Script 웹앱 (배포: "나로 실행 / 링크 있는 모든 사용자")
- 보안: URL 비공개 + 스크립트 상수 KEY 문자열 일치 검사. 이 이상의 인증은 스코프 밖

### 7.2 API 계약
- **저장** `POST {webAppUrl}` — 본문은 `Content-Type: text/plain;charset=utf-8` 의 JSON 문자열 (preflight 회피용. application/json 쓰지 말 것)
  ```json
  {"key":"...","name":"종목A","market":"KR","entry":10000,"stop":9400,
   "splits":3,"ratio":2.0,"sells":"10,600 / 11,200 / 11,800",
   "budget":1000000,"ceiling":11500,"memo":"..."}
  ```
  응답: `{"ok":true}` 또는 `{"ok":false,"error":"키 불일치"}`
- **목록** `GET {webAppUrl}?key=...&action=list` — 응답 `{"ok":true,"rows":[[...], ...]}` (헤더 제외, 최신순, 최대 50건)
- fetch는 리다이렉트를 따라가야 함(기본값). 오류 시 사용자에게 토스트로 원인 표시

### 7.3 시트 스키마 (시트명 "매도사다리", 없으면 스크립트가 헤더와 함께 자동 생성)

`날짜 | 종목명 | 시장 | 매수가 | 손절가 | 분할수 | 손익비 | 매도가 | 예산 | 상한가 | 메모`

### 7.4 프론트 설정 저장
- 웹앱 URL과 키는 설정 영역에서 입력받아 localStorage(`ladder.gasUrl`, `ladder.gasKey`)에 기억. localStorage 접근은 try/catch로 감싸 실패해도 계산기는 동작
- URL/키 미설정 상태에서 저장·목록 클릭 → 설정 영역을 펼치고 안내 토스트

## 8. 기술 제약

- **단일 index.html** (CSS/JS 인라인), 빌드 도구·프레임워크 금지. 외부 의존성은 구글 폰트 정도만 허용 (폰트 실패 시 시스템 폰트 폴백)
- 산출물: `index.html`, `apps-script.gs`, `README.md`(배포 절차 포함)
- 참고 프로토타입이 있다면(index.html / apps-script.gs 초안) 로직 검증의 출발점으로 삼되, 이 문서가 우선

## 9. 스코프 제외 (만들지 말 것)

- 상태 관리(계획→진입→종료 추적) — 추후 매매일지 프로젝트와 통합 시 검토
- 실시간 시세 조회, 자동 주문, 증권사 연동
- 분할매수 계산 (별도 스프레드시트 v2.1이 담당)
- 로그인/OAuth, 다중 사용자
- 저장 데이터 수정·삭제 UI (시트에서 직접 하면 됨)

## 10. 검증 벡터 (구현 후 이 값과 일치해야 함)

기본: E=10,000, S=9,400 (R=600), 시장 KR

| # | 입력 | 기대 출력 |
|---|---|---|
| 1 | k=2.0, 3분할 | 매도가 10,600 / 11,200 / 11,800 · 비중 33/33/34% · 필요 상승률 +18.0% · 본전 승률 33% · 손절폭 6.0% |
| 2 | k=2.5, 3분할 | 10,600 / 11,500 / 12,400 |
| 3 | k=2.0, 2분할 | 10,600 / 11,800 (50/50%) |
| 4 | k=2.0, 1분할 | 11,200 (100%) |
| 5 | k=2.0, 3분할, C=11,500 | 1·2차 "이내", 3차(11,800) "초과" → k_max = ((11500−10000)/600+1)/2 = 1.75 → **1.7** 제안 메시지 |
| 6 | k=2.0, 3분할, 예산 1,000,000 | 총 100주(33/33/34) · 매수 1,000,000원 · 손절 시 **−62,910원** · 전량 익절 시 **+117,419원** (매도합 1,120,600, 수수료 각각 2,910 / 3,181) |
| 7 | S=10,500 (>E) | 결과 미출력 + 에러 문구 |
| 8 | 손절폭 케이스 | S=9,850(1.5%) → "노이즈 주의" / S=8,900(11%) → "타점 늦음" |
| 9 | C=9,800 (≤E) | "들어갈 이유가 없습니다" 문구 |
| 10 | US, E=100, S=94, k=2.0, 3분할 | 106.00 / 112.00 / 118.00 (센트 내림, 소수 2자리 표기) |

수용 기준: 위 10개 벡터 전부 통과 + 모바일 뷰포트(390px)에서 가로 스크롤 없음 + 저장/목록이 실제 Apps Script 배포본과 왕복 성공.

## 11. 배포 절차 (README.md에 포함시킬 내용)

1. **Apps Script**: 새 구글시트 → 확장 프로그램 → Apps Script → `apps-script.gs` 붙여넣기 → 상수 KEY를 본인만의 문자열로 변경 → 배포 → 새 배포 → 유형 "웹 앱" → 실행: 나 / 액세스: 링크가 있는 모든 사용자 → 웹앱 URL 복사
2. **GitHub Pages**: 리포에 index.html 푸시 → Settings → Pages 활성화
3. **연결**: 사이트의 "저장 설정"에 웹앱 URL과 KEY 입력 (브라우저에 기억됨)
4. 코드 수정 후 Apps Script는 "배포 관리 → 새 버전"으로 갱신해야 반영됨을 README에 명시할 것

## 12. 결정 이력 요약 (변경하려면 발주자와 재논의)

- 사다리 구조: 균등 비중 + 1차 +1R + 등간격 (대안이던 러너 차등 비중, 간격 직접 입력은 기각)
- 저장 범위: 스냅샷 + 목록 (상태 관리 기각)
- 현실 상한가: 선택 입력으로 채택
- 도구 형태: 시트 아닌 웹 (계산 단순·모바일 사용·실시간 슬라이더가 결정 근거)
- 저장소: 구글시트 + Apps Script (클로드 세션을 저장소로 쓰는 안은 기각 — 웹에서 호출 불가, 대화는 장부가 아님)
